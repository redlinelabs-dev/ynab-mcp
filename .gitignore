node_modules
dist
.astro

.env

# Local agent scratch / process artifacts (not project code)
Plans/
MEMORY/
.atrium/
.mcp.json

# Local agent state (worktrees, local settings) — but skills are versioned
.claude/*
!.claude/skills/
